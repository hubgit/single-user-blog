{
  "rules": {
    "public": {
      ".read": true,
      ".write": "root.child('users').child(auth.uid).child('admin').val() === true",
      "metadata": {
        ".indexOn": "published"
      }
    },
    "queue": {
      ".read": "root.child('users').child(auth.uid).child('admin').val() === true",
      "metadata": {
        "$id": {
          ".write": "auth.uid != null && auth.uid == newData.child('owner').val() && (!data.exists() || auth.uid == data.child('owner').val())"
        },
        ".indexOn": "queued"
      },
      "content": {
        "$id": {
          ".write": "auth.uid != null && auth.uid == newData.child('owner').val() && (!data.exists() || auth.uid == data.child('owner').val())"
        }
      }
    },
    "private": {
      ".read": "root.child('users').child(auth.uid).child('admin').val() === true",
      "$uid": {
        ".read": "auth.uid == $uid",
        ".write": "auth.uid == $uid",
        "metadata": {
          ".indexOn": "created"
        }
      }
    },
    "users": {
      ".read": "root.child('users').child(auth.uid).child('admin').val() === true",
      "$uid": {
        ".read": "auth.uid == $uid",
        ".write": false
      }
    }
  }
}

export default theme => ({
  main: {
    display: 'flex',
    justifyContent: 'center',
  },
  intro: {
    padding: 100,
  },
  create: {
    position: 'fixed',
    bottom: theme.spacing.unit * 2,
    right: theme.spacing.unit * 2,
  }
})